//需求：已有数组var nums = [5,10,15],要求创建一个新数组
    //1.新数组的长度和已知数组相同
    //2.新数组每个元素的值，是已知对应位置元素的2倍
    //3.在控制台中打印新数组的所有元素 
    //步骤
    var nums = [5,10,15]
    //1.创建新数组
    var num=[]
    //2.遍历数组
    for(a=0;a<nums.length;a++){
        //2.1取出旧数组的元素并且 * 2 赋值给新数组
        num[a]=nums[a]*2
    }
    //3.打印
    console.log(num)
